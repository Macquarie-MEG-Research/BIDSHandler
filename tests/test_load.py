def test_load():
    # test the loading of a folder into the BIDS hierarchy

    # loading we can just throw the folder into a BIDSFolder object and then
    # poll some properties and make sure they are correct.
    pass


def test_copy():
    # test the copying of one BIDS component into another hierarchy.

    # we will need to copy the destination tree to a temporary folder like how
    # MNE and MNE-bids do their testing.
    # this temp folder can then be loaded into a BIDSFolder and the copy can be
    # done.
    pass
